<?php

namespace System\Data;

class htmlHelper
{
    /*##################################################################################################################
        class member
    ##################################################################################################################*/

    /*##################################################################################################################
        constructor
    ##################################################################################################################*/
    function __construct() { }

    /*##################################################################################################################
        function prepareForDatabase($array, $characterSet)

        @summary    convert values for database
        @access     public
        @param      $name (type of string) - name of created dropdown
        @param      $select (type of string) - selected value
        @param      $array (type of array) - list of possible values
        @param      $key (type of string) - name of field holding key
        @param      $value (type of string) - name of field holding value
        @param      $additional (type of string) - additional info for dropdown
        @return     boolean
    ##################################################################################################################*/
    public function dropDown($name, $select, $array, $key, $value, $additional) {
        if(is_array($array)) {

        }

        return $dropDown;
    }
}
?>